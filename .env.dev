# CogniVault Development Environment Configuration
# Copy this to .env and update with your actual OpenAI API key

# Database Configuration
POSTGRES_DB=cognivault
POSTGRES_USER=cognivault
POSTGRES_PASSWORD=cognivault_dev

# OpenAI Configuration
OPENAI_API_KEY=your-openai-api-key-here
OPENAI_MODEL=gpt-4

# Application Configuration
LOG_LEVEL=DEBUG

# Redis Configuration (uses defaults)
# REDIS_URL=redis://redis:6379/0

# Usage Instructions:
# 1. Copy this file: cp .env.dev .env
# 2. Update OPENAI_API_KEY with your actual key
# 3. Run: docker compose --env-file .env -f docker-compose.dev.yml up --build
# 4. Optional tests: docker compose --env-file .env -f docker-compose.dev.yml --profile test up tests