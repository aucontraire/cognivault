[mypy]
python_version = 3.12
explicit_package_bases = True
mypy_path = src
plugins = pydantic.mypy

# Production code settings (maintain current standards)
[mypy-src.cognivault.*]
# Current production settings are working well

# Test code strict settings
[mypy-tests.*]
# Strict typing for tests to catch interface violations  
strict = True
